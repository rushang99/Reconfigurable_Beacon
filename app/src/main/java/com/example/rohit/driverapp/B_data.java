package com.example.rohit.driverapp;

public class B_data {
    public String B_name;
    public int B_major;
    public int B_minor;
    public String B_uuid;

    public B_data(String name, int major, int minor, String uuid)
    {
        B_name.equals(name);
        B_major=major;
        B_minor=minor;
        B_uuid.equals(uuid);
    }

    public void update_info(String name, int major, int minor, String uuid)
    {
        B_name.equals(name);
        B_major=major;
        B_minor=minor;
        B_uuid.equals(uuid);
    }
}
